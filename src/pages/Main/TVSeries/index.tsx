import { IonContent } from '@ionic/react'
import React from 'react'

type Props = {}

const TVSeries: React.FC = (props: Props) => {
  return <IonContent>TVSeries</IonContent>
}

export default TVSeries
